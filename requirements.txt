# 基本分析ライブラリ
pandas>=1.3.0,<2.0.0    # 2.0.0以降は破壊的変更がある可能性あり
numpy>=1.20.0,<1.25.0   # pandasの依存関係と合わせる
scikit-learn>=1.0.0,<1.3.0  # 安定バージョンに制限

# データ取得
yfinance>=0.1.70,<0.2.0  # 上限を指定して互換性確保
requests>=2.26.0,<3.0.0
beautifulsoup4>=4.10.0,<5.0.0

# テクニカル分析
ta>=0.10.0,<0.11.0      # バージョン範囲を制限

# 可視化
matplotlib>=3.4.0,<3.7.0  # 3.7.0以降はAPIに変更があるため制限
seaborn>=0.11.2,<0.13.0

# データ処理・並列化
joblib>=1.1.0,<2.0.0     # 並列処理のため追加
psutil>=5.8.0,<6.0.0     # システムリソース監視のため追加

# ネットワーク分析（クラスタリングに使用）
networkx>=2.6.0,<3.0.0

# クラスタリング関連
scikit-tda>=0.1.0        # トポロジカルデータ分析
# 他のTDA関連ライブラリは不使用
# umap-learn>=0.5.1     # 使用しない
# hdbscan>=0.8.27       # 使用しない

# テスト
pytest>=6.2.5,<7.0.0

# ノートブック
jupyter>=1.0.0
ipykernel>=6.0.0        # Jupyter連携のため追加
notebook>=6.4.0         # Jupyter Notebookのため追加

# エラー処理・ロギング
tqdm>=4.62.0,<5.0.0     # 進捗バーのため追加

# 型ヒント
typing-extensions>=4.0.0,<5.0.0  # 高度な型ヒントのため追加

# ドキュメント生成
# sphinx>=4.3.0          # 必要に応じてコメント解除

# 開発ツール（オプション）
# black>=22.3.0          # コード整形
# flake8>=4.0.0          # リンター
# mypy>=0.910            # 型チェック
